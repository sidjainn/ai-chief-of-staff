# Auto-generated logs — local only, never commit (may contain doc IDs / personal data).
# No trailing slash so symlinks (to private repo) are also ignored.
logs
/logs/

# Environment variables — never commit
.env
.env.local
*.env

# OAuth tokens — never commit
token.json
credentials.json
client_secret*.json
*_token.json
*_credentials.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc

# Job research — personal, not for repo 
jobs
/jobs/

# Weekly coach — personal, not for repo
weeks
/weeks/

# Immunity map — deeply personal (competing commitments / big assumptions), not for repo
maps
/maps/

# Email triage runs — personal, not for repo 
email-runs
/email-runs/

# Email triage real context — personal (priorities/team/voice). example.context/ is the public template.
# Now lives in ai-chief-of-staff-private; symlinked back. No trailing slash so symlink also ignored.
.claude/skills/email-triage/context
.claude/skills/email-triage/context/

# Shopping context — personal (profile/inventory/interests/budget/data-sources). Lives in private repo via symlink.
.claude/skills/shopping-context
.claude/skills/shopping-context/

# Playwright MCP scratch dir
.playwright-mcp/
.claude/skills/weekly-coach/charter-pillar-modes.md

# Shopping reccos output — lives in private repo via symlink
.shopping
/.shopping/
